// Ce fichier est conservé par convention NestJS mais n'est plus utilisé directement.
// La logique métier est dans les modules dédiés (auth, users, rooms, messages, chat).
import { Injectable } from '@nestjs/common';

@Injectable()
export class AppService {}
