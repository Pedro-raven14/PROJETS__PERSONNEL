import { Controller } from '@nestjs/common';

/**
 * Controller vide - migré vers PreferencesController (src/preferences/preferences.controller.ts)
 * Routes actives : GET /preferences et PATCH /preferences
 */
@Controller()
export class ParametresController {}
